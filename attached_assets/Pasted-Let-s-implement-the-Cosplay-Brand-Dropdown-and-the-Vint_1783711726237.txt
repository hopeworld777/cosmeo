Let's implement the Cosplay Brand Dropdown and the Vinted-style "Real Photos Only" disclaimer for our listing creation form. 

Please perform the following steps across our codebase:

1. Database Schema Update:
   - Add a 'brand' column (VARCHAR) to our 'listings' table. Make sure it is added safely using an idempotent migration guard (e.g., 'ALTER TABLE listings ADD COLUMN IF NOT EXISTS brand VARCHAR(255);') so it runs safely on our active database.

2. Add the Vinted-Style "Real Photos Only" Warning:
   - In 'src/pages/CreateListing.jsx' (or our active listing form component), add a prominent warning banner right near the image upload section.
   - It should be styled beautifully with a soft warning border and background (using Tailwind like 'bg-amber-500/10 border border-amber-500/30 text-amber-200' or matching dark theme tokens) and include an alert/warning icon.
   - Georgian Text: "გაფრთხილება: გთხოვთ ატვირთოთ კოსპლეის რეალური ფოტოები. სტოკ-ფოტოების ან ბრენდის ოფიციალური სურათების გამოყენების შემთხვევაში, თქვენი განცხადება დაიბლოკება და წაიშლება!"
   - English Text: "Warning: Please upload real photos of your cosplay. Using stock or official brand photos will result in your listing being flagged and removed!"

3. Add the "Cosplay Brand / Maker" Dropdown:
   - Add a new selection dropdown field labeled "ბრენდი / კრეატორი" (Brand / Maker) to the form.
   - Populated the dropdown with these highly recognized cosplay options:
     * DokiDoki (SSR)
     * DokiDoki (SR / R)
     * 1/3 Delusion (三分妄想)
     * Uwowo Cosplay
     * Dragon Essence (龙途)
     * Miccostumes
     * EZCosplay
     * ProCosplay
     * RoleCosplay / Holoun
     * Self-Made / Handcrafted (ჩემი ხელით შექმნილი)
     * Other / Unbranded (სხვა)
   - Ensure the selected brand is properly included in the form state and sent to the POST backend endpoint when saving a new listing.

4. Update the Listing Details Page:
   - In 'src/pages/ItemDetail.jsx' (or our active listing details view), display the chosen brand cleanly.
   - If a brand is selected, render it as a sleek, stylized badge (e.g., a subtle gray or purple capsule next to the category badge) so buyers immediately know the maker of the cosplay.

Please run the database migration, update the React files, and verify that a test listing can be created and loaded with the new brand field successfully!